type Item {
    id: ID!
    name: String!
    description: String
}

input ItemInput {
    name: String!
    description: String
}

type Query {
    getItems: [Item]
}

type Mutation {
    addItem(input: ItemInput!): Item
}
